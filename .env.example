# ==============================================
# The Yellow Balloon - Environment Variables
# ==============================================
# Copy this file to .env and update with your actual values

# ----------------------------------------------
# Database Configuration
# ----------------------------------------------
# PostgreSQL database connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example: postgresql://user:password@localhost:5432/yellow_balloon_db
DATABASE_URL=postgresql://username:password@host:port/database

# ----------------------------------------------
# Server Configuration
# ----------------------------------------------
# Port for the Express server (default: 5000)
# The frontend Vite dev server proxies to this port
PORT=5000

# Node environment (development, production, test)
# This is automatically set by npm scripts, but can be overridden
NODE_ENV=development

# ----------------------------------------------
# Replit Configuration (Optional)
# ----------------------------------------------
# These are automatically set by Replit platform
# Only needed when deploying on Replit
# REPL_ID=your-replit-id
# REPLIT_DB_URL=your-replit-db-url

# ----------------------------------------------
# Notes:
# ----------------------------------------------
# 1. Never commit the .env file to version control
# 2. Keep .env.example up to date when adding new variables
# 3. Database URL should use SSL in production (sslmode=require)
# 4. For local development, you can use a local PostgreSQL instance
# 5. For Replit deployment, DATABASE_URL is managed via Neon integration
